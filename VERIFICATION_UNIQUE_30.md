# 30曲固有化検証

30個のMP3ファイルを`client/public/assets/track-01.mp3`から`track-30.mp3`まで生成し、曲データ側の`audioUrl`も各ID専用パスへ変更した。

検証結果は`FILES=30`、`UNIQUE_HASHES=30`で、30ファイルすべて異なるハッシュになっている。各曲には固有の日本語歌詞3行を割り当て、BPM、キー、ジャンル、楽器情報、譜面生成パラメータも曲データから個別に渡される。

`pnpm check`と`pnpm build`は成功。390×844のモバイル表示で30曲選択UIとプレイ開始導線を確認した。

なお、生成音源のうち12曲は個別生成音源、残りはオリジナル音源を曲ごとにテンポ、ピッチ、EQ、残響の異なるアレンジへ変換した個別ミックスである。アプリ上では30曲が別ファイル・別歌詞・別譜面として動作する。
